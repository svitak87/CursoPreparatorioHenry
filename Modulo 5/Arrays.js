//var listaDeCompras = [];
//listaDeCompras[0] = "tomates";
//listaDeCompras[1] = "lechuga", "pepinos";
//listaDeCompras[2] = "zanahoria";

//var elementoDelArray = listaDeCompras[0];

//console.log(listaDeCompras.length);

//var nombres = ['Matias', 'Maria', 'Diego', 'Rosa'];
//nombres.pop();
//nombres.push('Oscar');
//nombres.unshift('Valeria');
//nombres.shift();
//console.log(nombres);

//function devolverPrimerElemento(array, elemento) {
    // Retornar el primer elemento del arreglo recibido por parámetro.
    // Tu código:

 
 //console.log(devolverPrimerElemento.length);

 //var numeros = [2, 7 , 9, 6, 7];
 //var cumpleCondicion = numeros.every ((num) => {
    //return num > 1;
 //})

 //console.log(cumpleCondicion);

//palabras = ['Henry', 'JavaScript', 'Class'];
//var palabrasAFrase = palabras.join(" ");
//console.log(palabrasAFrase);

//arreglo = [1, 6 , 7, 9];
//suma = 0

//for(var i = 0 ; i < arreglo.length ; i++){
    //suma += arreglo[i];
//}
//onsole.log(suma);

//resultadosTest = [ 4, 8, 2, 9, 10];
//notas = 0
//
 //   for (var i = 0 ; i < resultadosTest.length ; i++) {
  //     notas += resultadosTest[i];
    //}

//array = [4, 6, 7, 9, 10];
//producto = 1
        //for(var i = 0 ; i < array.length ; i++){
        //producto = producto * array[i];
        //}       
        //console.log(producto);
//array = [4, 5, 9, 7, 20, 45, 2, 48];
//var valores = 0;
//function cuentoElementos(array) {
// Desarrolla una función que retorne la cantidad de elementos del arreglo cuyo valor sea mayor que 18.
// Tu código:
//for(var i = 0 ; i < array.length ; i++){
   // if(array[i] > 18){
     //   valores += 1;
       //}  
    //}
     //console.log(array); 
//}


function diaDeLaSemana(numeroDeDia) {
    // Supongamos que los días de la semana se codifican como 1 = Domingo, 2 = Lunes y así sucesivamente.
    // Realiza una función que, dado el número del día de la semana, retorne: "Es fin de semana"
    // si el día corresponde a "Sábado" o "Domingo", y "Es dia laboral" en caso contrario.
    // Tu código:

//    if(numeroDeDia === 1 || numeroDeDia === 7){
//        return "Es fin de semana";
//    }else if(numeroDeDia > 1 && numeroDeDia < 7){
//        return "Es dia laboral";
//    }
// }
 //console.log(diaDeLaSemana(3));

// function empiezaConNueve(num) {
//    // Esta función recibe por parámetro un número.
    // Debe retornar true si el entero inicia con 9 y false en otro caso.
    // Tu código:
//    numeroString = num.toString();
//    if(numeroString [0] === "9"){
//        return true;
//    }else{
//        return false;
    }
    
//}
// console.log(empiezaConNueve(654));

function mesesDelAño(array) {
    // El arreglo contiene algunos meses del año desordenados. Debes recorrerlo, buscar los meses "Enero",
    // "Marzo" y "Noviembre", guardarlos en un nuevo arreglo y retornarlo.
    // Si alguno de los meses no está, retornar el string: "No se encontraron los meses pedidos".
    // Tu código:

